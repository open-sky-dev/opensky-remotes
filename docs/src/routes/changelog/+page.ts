import { metaLoad } from '@opensky/seo'

export const load = metaLoad.page({
	title: 'Changelog',
	description:
		'Release history for @opensky/remotes — every version, what changed, and the notes for migrating between them.'
})
