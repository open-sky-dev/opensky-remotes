import { metaLoad } from '@opensky/seo'

export const load = metaLoad.page({
	title: 'Draft persistence',
	description:
		'A draft persistence example: opted-in fields save to web storage as you type and restore after a reload, so a half-finished form survives.'
})
