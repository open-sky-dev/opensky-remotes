import { metaLoad } from '@opensky/seo'

export const load = metaLoad.page({
	title: 'Contact form',
	description:
		'A contact form example: inline validation with sync and async validators, server-only issues, and tracked submission state with delayed feedback.'
})
