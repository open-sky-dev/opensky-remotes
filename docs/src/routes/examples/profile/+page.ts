import { metaLoad } from '@opensky/seo'

export const load = metaLoad.page({
	title: 'Auto-save profile',
	description:
		'An auto-save profile example: the form submits itself once input settles, with no save button, using dirty checking and in-flight coalescing.'
})
